// import Image from "next/image";
import "./globals.css";

export default function Home() {
  return (
   <main className="bg-[hsl(var(--primary))] min-h-screen flex flex-col items-center justify-center text-text">
      <div className="min-h-screen text-[hsl(var(--primary))] flex items-center justify-center">
      <button className="btn-hero">Shop Now</button>
    </div>
    </main>
  );
}
